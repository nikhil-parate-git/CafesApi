# Application Name
spring.application.name=cafe-management-api

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# =========== PRODUCTION DATABASE CONFIG ===========
# DO NOT PUT DEFAULT VALUES! Railway will provide these
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection Pool Settings (Important for Railway)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Disable SQL initialization in production (use manually)
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Disable auto-create in production
spring.jpa.generate-ddl=falsegit add src/main/resources/application*.properties